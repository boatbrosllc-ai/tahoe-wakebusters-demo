# platform-dev

Default / neutral Slipstack demo site. Used when `SLIPSTACK_SITE_ID` is unset.

Homepage: `pages/home.tsx` (the original marketing layout).
Config: `config.ts`

Real customers should get their own sibling folder rather than editing this one into a branded operator.
