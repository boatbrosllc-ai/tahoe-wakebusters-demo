/**
 * Lake Austin Pontoon loading — staged skeleton (hero first, then content blocks).
 * Shown while pontoon page segment and optional Firestore data load.
 */
import { ExperiencePageSkeleton } from "@/components/site/ExperiencePageSkeleton";

export default function LakeAustinPontoonLoading() {
  return <ExperiencePageSkeleton />;
}
