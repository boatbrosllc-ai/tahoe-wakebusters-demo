[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Next.js API routes (/api/booking/slots, date-prices) use maxDuration=26 in route.ts.
  # If calendar only loads one month in production, set function timeout to 26s in Netlify UI (Site settings → Functions) or ask Netlify Support.
  # Allow long-running booking and cron functions up to 60 seconds
  # (Netlify default is 10s; background functions allow up to 15 min)
  included_files = ["netlify/functions/**"]

[functions."booking-reminder-cron"]
  timeout = 60

[functions."final-payment-reminder-cron"]
  timeout = 60

[functions."cleanup-holds"]
  timeout = 60

[functions."run-final-charges"]
  timeout = 60

[functions."waiver-reminder-cron"]
  timeout = 60

[functions."warm-booking"]
  timeout = 10
